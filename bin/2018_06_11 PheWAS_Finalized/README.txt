Finalized version of the PheWAS analysis program that includes markdown text in the Jupyter notebook for the Python script.

R program converted to a Jupyter Notebook with markdown text, and scatter plot updated to be sorted by PheWAS code group.

Essentially the same as the 2018_06_08 folder, but with absolute paths instead of relative paths.